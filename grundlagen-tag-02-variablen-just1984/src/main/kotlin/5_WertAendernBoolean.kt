fun main() {

    var isHapppy: Boolean = true
    println(isHapppy)
    print("Neuer Wert: ")

    // Verändere den Wert der Variable hier!
    isHapppy = false
    println(isHapppy)
}


//In den folgenden Aufgaben bekommst du eine bereits angelegte Variable mit einem Initialwert und sollst Code schreiben,
// der den Wert der Variable nachträglich ändert (also ohne etwas am bestehenden Code zu ändern).
//Schau dir den Code im Projekt an und gib der Variable den neuen Wert “true”.