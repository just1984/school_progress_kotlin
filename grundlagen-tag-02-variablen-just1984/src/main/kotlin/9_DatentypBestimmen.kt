fun main() {

// Setze an der richtigen Stelle den passenden Datentyp für diese Variable ein
    var productCount: Int = 10
    println(productCount)
}

// In dieser Aufgabe wird dir ein Code vorgegeben, der nicht ganz vollständig ist.
// Bei der Deklaration von Variablen sind Name und Wert gegeben, es fehlt allerdings der Datentyp.
// Schau dir den Code an und überlege, welcher Datentyp zu dieser Variable passt. Trage dann den richtigen Datentyp an der richtigen Stelle ein.