fun main() {
    //Unter diesem Kommentar deinen Code reinschreiben:
    //Nutze eine der heute gelernten Methoden,
    // um die kleinste Zahl in der Liste auszugeben.
    var liste: MutableList<`Int`> = mutableListOf(5,4,6,7)
    println(liste.min())
}