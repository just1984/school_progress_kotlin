fun main() {
    //Unter diesem Kommentar deinen Code reinschreiben:
    //Prüfe mit einer Methode aus der Vorlesung, ob die Liste leer ist oder nicht.
    // Es sollte true oder false ausgegeben werden. In diesem Fall sollte das Ergebnis false sein.

    var liste: List<Int> = listOf(5, 4, 6, 7)
    println(liste.size)
    println(liste.isEmpty())
}