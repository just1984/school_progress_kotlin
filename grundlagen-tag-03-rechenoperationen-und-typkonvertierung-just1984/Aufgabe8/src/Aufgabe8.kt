fun main() {

    var kg: Int = 10
    val precise_kg: Double = kg.toDouble()
    println(precise_kg)

    // Erstelle hier die neue Variable
    // Gehe nun unter src -> main -> kotlin in die Datei `Aufgabe8.kt`. Hier findest du bereits die Variable `kg`.
    // Erstelle nun eine neue Variable mit dem Namen `precise_kg` vom Typ Double und speichere in ihr den Wert aus der Variable `kg`.
    // Tue dies mit Hilfe der Typkonvertierung.

}